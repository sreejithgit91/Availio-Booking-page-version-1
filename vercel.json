{
  "buildCommand": "cd booking-app && npm install && npm run build",
  "outputDirectory": "booking-app/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
